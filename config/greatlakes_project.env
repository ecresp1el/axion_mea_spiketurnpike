#!/usr/bin/env bash
# Shared Great Lakes defaults for Axion MEA Kilosort jobs.

export REPO_ROOT="${REPO_ROOT:-/home/elcrespo/Desktop/githubprojects/axion_mea_spiketurnpike}"
export PROJECT_ROOT="${PROJECT_ROOT:-/nfs/turbo/umms-parent/axion_mea_spiketurnpike_projectfolder}"
export CONDA_BASE="${CONDA_BASE:-/home/elcrespo/miniconda3}"
export CONDA_ENV="${CONDA_ENV:-${PROJECT_ROOT}/envs/axion-kilosort}"
export CONDA_PKGS_DIRS="${CONDA_PKGS_DIRS:-${PROJECT_ROOT}/conda_pkgs}"
export PIP_CACHE_DIR="${PIP_CACHE_DIR:-${PROJECT_ROOT}/pip_cache}"

export AXION_RAW_ROOT="${AXION_RAW_ROOT:-${PROJECT_ROOT}/data/raw}"
export KILOSORT_BINARY_ROOT="${KILOSORT_BINARY_ROOT:-${PROJECT_ROOT}/data/interim/kilosort_binary}"
export KILOSORT_RESULTS_ROOT="${KILOSORT_RESULTS_ROOT:-${PROJECT_ROOT}/results/kilosort}"
export KILOSORT_LOG_ROOT="${KILOSORT_LOG_ROOT:-${PROJECT_ROOT}/logs}"
export KILOSORT_JOBS_ROOT="${KILOSORT_JOBS_ROOT:-${PROJECT_ROOT}/jobs}"
export KILOSORT_SCRATCH_ROOT="${KILOSORT_SCRATCH_ROOT:-${PROJECT_ROOT}/scratch}"

export KEMPNER_REPO_ROOT="${KEMPNER_REPO_ROOT:-/home/elcrespo/Desktop/githubprojects/ephys-spike-sorting}"
export KEMPNER_NEXTFLOW_BIN="${KEMPNER_NEXTFLOW_BIN:-${PROJECT_ROOT}/tools/nextflow}"
export KEMPNER_CONTAINER_DIR="${KEMPNER_CONTAINER_DIR:-${PROJECT_ROOT}/containers/kempner_ephys}"
export KEMPNER_NWB_INPUT_ROOT="${KEMPNER_NWB_INPUT_ROOT:-${PROJECT_ROOT}/data/interim/kempner_nwb_inputs}"
export KEMPNER_RESULTS_ROOT="${KEMPNER_RESULTS_ROOT:-${PROJECT_ROOT}/results/kempner}"
export KEMPNER_WORK_ROOT="${KEMPNER_WORK_ROOT:-${PROJECT_ROOT}/scratch/kempner_nextflow}"
export KEMPNER_JOBS_ROOT="${KEMPNER_JOBS_ROOT:-${PROJECT_ROOT}/jobs/kempner}"
export KEMPNER_LOG_ROOT="${KEMPNER_LOG_ROOT:-${PROJECT_ROOT}/logs/kempner}"
export KEMPNER_SINGULARITY_CACHE_DIR="${KEMPNER_SINGULARITY_CACHE_DIR:-${PROJECT_ROOT}/scratch/singularity_cache}"
export KEMPNER_SINGULARITY_TMPDIR="${KEMPNER_SINGULARITY_TMPDIR:-${PROJECT_ROOT}/scratch/singularity_tmp}"
export KEMPNER_CPU_PARTITION="${KEMPNER_CPU_PARTITION:-standard}"
export KEMPNER_GPU_PARTITION="${KEMPNER_GPU_PARTITION:-gpu}"
export KEMPNER_ACCOUNT="${KEMPNER_ACCOUNT:-parent0}"
export KEMPNER_GPU_GRES="${KEMPNER_GPU_GRES:-gpu:1}"
export KEMPNER_OPENJDK_MODULE="${KEMPNER_OPENJDK_MODULE:-openjdk/21.0.1}"
export KEMPNER_SINGULARITY_MODULE="${KEMPNER_SINGULARITY_MODULE:-singularity/4.4.1}"

export PLATE_MAP_24="${PLATE_MAP_24:-${REPO_ROOT}/metadata/plate_maps/axion_24_well_plate_map.csv}"
export PLATE_MAP_48_OPTO="${PLATE_MAP_48_OPTO:-${REPO_ROOT}/metadata/plate_maps/axion_48_well_opto_plate_map.csv}"
export ELECTRODE_GEOMETRY_4X4="${ELECTRODE_GEOMETRY_4X4:-${REPO_ROOT}/metadata/plate_maps/axion_per_well_4x4_electrode_geometry.csv}"
